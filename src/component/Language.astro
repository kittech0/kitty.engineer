<div id="lang-selector">
    <button id="lang-btn" aria-label="Select language" class="neon-font"
        >🇬🇧</button
    >
    <div id="lang-list">
        <div data-lang="en">🇬🇧 English</div>
        <div data-lang="pl">🇵🇱 Polski</div>
    </div>
</div>

<style>
    #lang-selector {
        position: absolute;
        top: 1em;
        right: 1em;
    }

    #lang-btn {
        width: 2.5em;
        height: 2.5em;
        border-radius: 50%;
        display: grid;
        place-items: center;
        background-color: var(--main-bg);
        color: var(--text-color);
        box-shadow: var(--button-shadow-color) 0 0 10px 0;
        border: none;
        cursor: pointer;
        font-size: 1.4em;
        outline-offset: 0;
        transition:
            background-color 0.2s ease-in,
            transform 0.1s ease-in;

        &:hover {
            background-color: var(--link-visited-color);
            box-shadow: var(--button-shadow-color) 0 4px 8px;
            transform: scale(1.05);
        }

        &:active {
            background-color: var(--link-hover-color);
            box-shadow: var(--button-shadow-color) 0 1px 3px;
            transform: scale(0.95);
        }

        &:focus-visible {
            outline: 2px solid var(--link-hover-color);
            outline-offset: 2px;
        }
    }

    #lang-list {
        display: none;
        position: absolute;
        top: 3em;
        right: 0;
        background-color: var(--main-bg);
        color: var(--text-color);
        box-shadow: var(--button-shadow-color) 0 0 10px 0;
        border: none;
        cursor: pointer;
        outline-offset: 0;
        transition:
            background-color 0.2s ease-in,
            transform 0.1s ease-in;
    }

    #lang-list div {
        padding: 0.5em 1em;
        white-space: nowrap;
    }

    #lang-list div:hover {
        background: var(--link-visited-color);
    }

    #lang-selector.open #lang-list {
        display: block;
    }
</style>
