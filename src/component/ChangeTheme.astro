<button aria-label="Toggle dark mode" id="mode-toggle" class="neon-font"
    >☀️</button
>

<style lang="scss">
    #mode-toggle {
        position: absolute;
        top: 1em;
        left: 1em;
        width: 2.5em;
        height: 2.5em;
        border-radius: 50%;
        display: grid;
        place-items: center;
        background-color: var(--main-bg);
        color: var(--text-color);
        box-shadow: var(--button-shadow-color) 0 0 10px 0;
        border: none;
        cursor: pointer;
        font-size: 1.4em;
        outline-offset: 0;
        transition:
            background-color 0.2s ease-in,
            transform 0.1s ease-in;

        &:hover {
            background-color: var(--link-visited-color);
            box-shadow: var(--button-shadow-color) 0 4px 8px;
            transform: scale(1.05);
        }

        &:active {
            background-color: var(--link-hover-color);
            box-shadow: var(--button-shadow-color) 0 1px 3px;
            transform: scale(0.95);
        }

        &:focus-visible {
            outline: 2px solid var(--link-hover-color);
            outline-offset: 2px;
        }
    }
</style>
